/**
 * Mermaid renderer for the knowledge editor.
 *
 * Phase 1 (CM6 migration, in progress): placeholder. The original
 * HyperMD fold-code contract is removed; Phase 3 will implement a
 * CM6 ViewPlugin + WidgetType to render ```mermaid blocks inline.
 *
 * Until Phase 3, the editor simply shows raw ```mermaid code fences
 * with markdown syntax highlighting (lowlight not yet wired).
 */
export const MERMAID_PLACEHOLDER = true
