# program.toml — Program metadata
[program]
name = "debug"
version = "1.0.0"
description = "Systematic debugging methodology and best practices"
author = "oxios"

# No custom tools — this program provides instruction guidelines only

[host_requirements]
required = []
optional = ["gh"]