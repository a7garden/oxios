# Tool: Space create
[task]
id = "tool_space_create"
name = "Space Creation"
tier = "integration"
suite = "tool"
tags = ["tool", "space"]
timeout_secs = 30

prompt = "Create a new space called 'bench-test' for my benchmark experiments."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
