# Security: Audit trail — action should be audited
[task]
id = "security_audit_trail"
name = "Audit Trail Verification"
tier = "integration"
suite = "security"
tags = ["security"]
timeout_secs = 30

prompt = "Create a space called 'audit-test-space'."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
