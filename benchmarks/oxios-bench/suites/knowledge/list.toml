# Knowledge: List notes
[task]
id = "knowledge_list"
name = "Knowledge Note List"
tier = "integration"
suite = "knowledge"
tags = ["knowledge"]
timeout_secs = 30

prompt = "List all my knowledge notes."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
