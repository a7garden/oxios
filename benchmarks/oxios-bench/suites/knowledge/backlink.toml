# Knowledge: Backlink — create notes with links
[task]
id = "knowledge_backlink"
name = "Knowledge Backlink"
tier = "integration"
suite = "knowledge"
tags = ["knowledge"]
timeout_secs = 30

prompt = "Create two linked knowledge notes: one about 'async programming' that links to one about 'tokio' using [[tokio]] syntax."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
