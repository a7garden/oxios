# Memory: Protect — protect a memory entry
[task]
id = "memory_protect"
name = "Memory Protection"
tier = "integration"
suite = "memory"
tags = ["memory"]
timeout_secs = 30

prompt = "Save 'secret_token = abc123' to memory and protect it so it won't be deleted."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
