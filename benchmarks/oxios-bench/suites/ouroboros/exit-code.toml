# Ouroboros: Exit code — successful task returns exit code 0
[task]
id = "ouroboros_exit_code"
name = "Exit Code Verification"
tier = "integration"
suite = "ouroboros"
tags = ["smoke"]
timeout_secs = 30

prompt = "Say 'pong'."

[expect]
exit_code = 0
response_not_contains = ["error"]
