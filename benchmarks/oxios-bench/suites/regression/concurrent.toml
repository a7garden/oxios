# Regression: Concurrent request — rapid sequential requests
[task]
id = "regression_concurrent"
name = "Concurrent Request Handling"
tier = "integration"
suite = "regression"
tags = ["regression"]
timeout_secs = 30

prompt = "Tell me the current date and time."

[expect]
exit_code = 0
response_not_contains = ["panic", "crash"]
