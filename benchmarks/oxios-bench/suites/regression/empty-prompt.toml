# Regression: Empty prompt — should not crash
[task]
id = "regression_empty_prompt"
name = "Empty Prompt Handling"
tier = "integration"
suite = "regression"
tags = ["regression"]
timeout_secs = 15

prompt = " "

[expect]
exit_code = 0
response_not_contains = ["panic", "crash"]
