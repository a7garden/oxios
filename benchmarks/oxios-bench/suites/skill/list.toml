# Skill: Skill listing — list available skills
[task]
id = "skill_list"
name = "Skill Listing"
tier = "integration"
suite = "skill"
tags = ["skill", "smoke"]
timeout_secs = 30

prompt = "List all available skills."

[expect]
exit_code = 0
response_not_contains = ["error", "panic"]
