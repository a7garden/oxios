# Agent: A2A query — ask other agents
[task]
id = "agent_a2a_query"
name = "A2A Query"
tier = "integration"
suite = "agent"
tags = ["agent"]
timeout_secs = 60

prompt = "Query other agents to find if any of them know about Rust programming."

[expect]
exit_code = 0
response_not_contains = ["panic", "crash"]
