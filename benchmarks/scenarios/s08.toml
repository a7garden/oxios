[scenario]
id = "s08"
name = "설정 변경"
tier = 2
difficulty = "medium"
time_limit_secs = 60
goal = "oxios 설정에서 게이트웨이 포트를 8080으로 변경해줘"

[evaluation]
min_expected = "설정 변경 명령이 성공해야 함. 변경 전후 값이 보고되어야 함"
quality_expected = "변경이 실제로 적용되었는지 확인하고, 원래 값도 함께 보고해야 함"
