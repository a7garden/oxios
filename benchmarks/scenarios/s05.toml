[scenario]
id = "s05"
name = "JSON 출력"
tier = 1
difficulty = "easy"
time_limit_secs = 60
goal = "JSON 형태로 한국의 수도, 인구(약 5200만), 면적(약 10만km²) 정보를 구조화해서 알려줘"

[evaluation]
min_expected = "유효한 JSON 형태로 응답. 수도는 서울, 인구와 면적이 대략 맞아야 함"
quality_expected = "JSON이 올바른 구조이고, 값이 정확하며, 추가 맥락이 있으면 가산점"
