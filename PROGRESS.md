# Progress

## Status
In Progress

## Tasks
- [x] RFC-009 Phase 5: Web UI Skills tab redesign, remove Programs + Host Tools tabs

## Files Changed
- `channels/oxios-web/web/src/types/index.ts` — Removed `Program` interface and old `Skill` interface. Added RFC-009 unified types: `SkillSource`, `SkillStatus`, `SkillRequirements`, `SkillInstallSpec`, `Skill` (with all fields from Section 5.1)
- `channels/oxios-web/web/src/routes/skills.tsx` — Full rebuild: filter tabs (All/Ready/Needs Setup/Disabled with counts), search by name/description, status badges (🟢🟡🔴), source badges, requirements display with ✅/❌ per item, missing summary, install specs, config checks, hover state, empty state. Uses `GET /api/skills` returning `{ skills: Skill[] }`
- `channels/oxios-web/web/src/routes/programs.tsx` — Deleted (was already removed from filesystem)
- `channels/oxios-web/web/src/routes/host-tools.tsx` — Deleted (was already removed from filesystem)
- `channels/oxios-web/web/src/components/layout/sidebar.tsx` — Removed Programs link (Cpu icon), Host Tools link (Wrench icon), and their unused lucide imports
- `channels/oxios-web/web/src/routeTree.gen.ts` — Auto-regenerated by TanStack Router plugin; no longer references programs or host-tools routes

## Notes
- `tsc --noEmit` passes with zero errors
- `bun run build` succeeds (528ms)
- Programs and Host Tools route files were already deleted from filesystem before this task
- `Skill` type no longer has `content` field (backend API returns structured metadata instead)
- Badge component already had `success`, `warning`, `destructive` variants needed for status display
