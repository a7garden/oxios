[package]
name = "oxios-memory"
version = "1.0.2"
edition = "2021"
description = "Tiered agent memory — extracted from oxios-kernel per RFC-018 b.1"
license = "MIT"

[dependencies]
serde = { workspace = true }
async-trait = { workspace = true }
serde_json = { workspace = true }
anyhow = { workspace = true }
